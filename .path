# get my own commands
export PATH=$DOTFILESDIR/bin:$PATH

# make sure that brew-stuff is used instead of system-stuff
export PATH=/usr/local/bin:$PATH

# needed when node is installed by brew; see http://thechangelog.com/install-node-js-with-homebrew-on-os-x/
#export PATH="/usr/local/bin:$PATH"
