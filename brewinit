brew update
brew upgrade
brew cleanup

HOMEBREW_FORMULAE=(
    "bash-completion"
    "git"
    "vim --override-system-vi"
    "nmap"
)

for i in ${!HOMEBREW_FORMULAE[*]}; do
	tmp="${HOMEBREW_FORMULAE[$i]}"
	echo $i
	echo $tmp
	$(brew install $tmp)
done