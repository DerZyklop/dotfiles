function commandExists() {
  if ! foobar_loc="$(type -p "$1")" || [ -z "$foobar_loc" ]; then
    return 1
  else
    return 0
  fi
}


function gp() {
  if [ $# -gt 1 ]; then
    echo "USAGE: gp [commit-msg]"
    tput setaf 1
    echo "Maximum one param!"
    tput sgr0
  elif [ $# -lt 1 ]; then
    echo "Push new commits"
    git push
    echo "Push all tags"
    git push --tags
  else
    echo "Commit"
    git commit -m $1
    echo "Push new commits to remote"
    git push
    echo "Push all tags to remote"
    git push --tags
  fi
}

function gi() {
  git init
  touch README.md
  touch .gitignore
  echo -e ".gitignore\n.DS_Store"> .gitignore
}

# color diffs for SVN
function svndiff () {
  svn diff ${1+"$@"} | colordiff
}


### NAVIGATION ###

function p() {
  if [ $# -gt 1 ]; then
    echo "USAGE: p projectname"
    tput setaf 1
    echo "Maximum one param!"
    tput sgr0
  elif [ $# -lt 1 ]; then
    echo "USAGE: p projectname"
    tput setaf 1
    echo "Minimum one param!"
    tput sgr0
  else

    # TODO: Implement this when projects are moved
    found_it=false
    for i in ${PROJECTDIRS[*]}; do
      if [ -d $i$1 ]; then
        cd $i$1
        found_it=true
      fi
      if [ -f $i$1 ]; then
        unzip -q $i$1 -d $i
        cd $i${1%.*}
        rm -f $i$1
        found_it=true
      fi
    done;
    if [[ "$found_it" = false ]]; then
      tput setaf 1
      echo "-------------------------"
      echo "Error: Project not found!"
      echo "-------------------------"
      tput sgr0
    fi

  fi
}
