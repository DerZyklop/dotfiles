#!/bin/sh

# Opens my personal Terminal configuration
function profile() {
  if [ -f ~/.bashrc ]; then
    subl ~/.bashrc
  fi

  if [ -f ~/.zshrc ]; then
    subl ~/.zshrc
  fi

  if [ -f ~/.profile ]; then
    subl .profile
  fi
}

profile
