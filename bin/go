#!/bin/sh

if [ -f boilerplate.json ]; then
  grunt dev
elif [ -f gruntfile.coffee ]; then
  grunt server
else
  tput setaf 1
  echo "--------------------------------------"
  echo "Error: Is this a Project with Grunt???"
  echo "--------------------------------------"
  tput sgr0
fi
